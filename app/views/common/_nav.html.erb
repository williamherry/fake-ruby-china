<%= render_list :class => "nav" do |li|
  li << link_to(t("menu.topics"), topics_path)
  li << link_to(t("menu.wiki"), '#')
  li << link_to(t("menu.sites"), '#')
  li << link_to(t("menu.users"), '#')
end %>
